# Shell scripts run inside Linux containers (docker-entrypoint-initdb.d, tools/*).
# A CRLF line ending breaks their shebang and heredocs there, so force LF regardless
# of a contributor's platform or core.autocrlf setting.
*.sh text eol=lf

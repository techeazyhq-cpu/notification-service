# Copyright 2026 Vasantha Kumar
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# @author Vasantha Kumar <vasantha.kumar@hotmail.com>
server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: notification-dispatcher
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/notification}
    username: ${DB_USER:notification_app}
    password: ${DB_PASSWORD:notification_app}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

notification:
  secrets-key: ${SECRETS_ENCRYPTION_KEY:development-only-change-me}
  data-key: ${DATA_ENCRYPTION_KEY:development-only-change-me}
  pulsar:
    service-url: ${PULSAR_URL:pulsar://localhost:6650}
    tls-trust-certs-file: ${PULSAR_TLS_TRUST_CERTS_FILE:}
  sweeper:
    enabled: true

retention:
  enabled: ${RETENTION_ENABLED:true}
  cron: ${RETENTION_CRON:0 30 2 * * *}
  personal-data-days: ${RETENTION_PERSONAL_DATA_DAYS:90}
  delete-days: ${RETENTION_DELETE_DAYS:400}
  idempotency-days: ${RETENTION_IDEMPOTENCY_DAYS:7}
  batch-size: ${RETENTION_BATCH_SIZE:5000}

billing:
  payment-terms-days: ${BILLING_PAYMENT_TERMS_DAYS:30}
  settlement:
    enabled: ${BILLING_SETTLEMENT_ENABLED:true}
  invoicing:
    enabled: ${BILLING_INVOICING_ENABLED:true}
    auto-issue: ${BILLING_AUTO_ISSUE:false}

dispatcher:
  consumers-per-channel: ${CONSUMERS_PER_CHANNEL:2}
  max-attempts: ${MAX_ATTEMPTS:5}
  base-backoff-seconds: ${BASE_BACKOFF_SECONDS:5}
  circuit-breaker:
    enabled: ${CB_ENABLED:true}
    sliding-window-size: ${CB_WINDOW:20}
    minimum-number-of-calls: ${CB_MIN_CALLS:10}
    failure-rate-threshold: ${CB_FAILURE_RATE:50}
    wait-duration-in-open-state-ms: ${CB_OPEN_WAIT_MS:30000}
    permitted-calls-in-half-open-state: ${CB_HALF_OPEN_CALLS:3}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,providerhealth
